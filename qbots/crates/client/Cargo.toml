[package]
name = "client"
version = "0.1.0"
edition.workspace = true

[dependencies]
bytes = "1"
q2proto = { path = "../q2proto" }
tokio = { version = "1", features = ["net", "time", "macros", "rt"] }
tracing = "0.1"
