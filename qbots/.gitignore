# Rust build output
/target/
/target-*/

# Cargo
**/*.rs.bk

# Vendored reference trees (cloned, not authored)
/vendor/

# Movement-scenario logs (generated by `spawn-to-spawn`/`spawn-to-weapon`, Plan 10)
/logs/

# Ahead-of-time nav graph cache (generated by `qbots generate-map-cache`, Plan 18)
/data/mapcache/

# Sisyphus / local tooling
/.sisyphus

# Local config (machine-specific paths); commit config.example.yaml instead
/config.yaml

# Editor / OS cruft
/.idea/
/.vscode/
*.swp
.DS_Store

# NOTE: Cargo.lock stays COMMITTED — qbots ships binaries.
