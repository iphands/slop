//! MCP server implementation for Wikipedia knowledge retrieval

/// Wikipedia MCP Server
pub struct WikiServer {
    // TODO: Add vector store reference for search
}

impl WikiServer {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for WikiServer {
    fn default() -> Self {
        Self::new()
    }
}
