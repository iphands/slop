# captures/

Trace and capture output. **Gitignored** — see RULES.md Rule E.

Naming: `<workload>_<date>_<what-varies>.<ext>` so a capture is identifiable months
later. An orphan `capture.gfxr` is a dead capture.
